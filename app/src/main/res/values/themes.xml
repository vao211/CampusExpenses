<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Theme cho light mode -->
    <style name="Theme.CampusExpenses" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Các thuộc tính theme của bạn -->
<!--        <item name="colorPrimary">@color/purple_500</item>-->
        <item name="colorPrimary">#EDA441</item>
        <item name="android:fontFamily">@font/roboto_medium</item>
    </style>

    <!-- Theme cho dark mode (nếu cần) -->
    <style name="Theme.CampusExpensesDark" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Các thuộc tính dark mode -->
    </style>
</resources>